<?php
/*-------------------------------------------------------
1. print your name on the screen
hint: the echo command prints something to the screen

---------------------------------------------------------
2. store a word (a string) into a variable, then print its value onto the screen
hint: starting a word with a dollar sign $ makes it a variable

---------------------------------------------------------
3. add more words to the variable you created in step 2, then print the final value onto the screen
hint: you use the dot operator (.) to combine (concatenate) 2 strings

---------------------------------------------------------
4. store a number into a variable, add 5 to it, then print the total to the screen
hint: += will add a value to a variable

---------------------------------------------------------
5. create an if/else statement to print different values based different situations
syntax: 
	if (<TRUE/FALSE EXPRESSION>) {
		// some code
	} elseif (<TRUE/FALSE EXPRESSION>) {
		// some code
	} else {
		// some more code
	}

---------------------------------------------------------
6. create a loop that prints the numbers from 1 to 10 each on a separate line
syntax: 
	for (<INITIALIZE>; <TRUE/FALSE EXPRESSION>; <COUNTER INCREMENT>) {
		// some code
	}
hint: you can concatenate a <br> onto your variable to generate a newline

---------------------------------------------------------
7. create an array to hold many variables
syntax to create: 
	$values = array(
		'key1' => 'value1', 
		'key2' => 'value2',
	);
syntax to access:
	$variable['key1']

---------------------------------------------------------
8. use a foreach loop to print each of the values of the array you created in step 7
syntax:
	foreach($values as $key => $value) {
		// do something with either $key or $value
	}

---------------------------------------------------------*/

// WRITE YOUR CODE HERE

?>
